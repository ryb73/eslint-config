name: "Install and Build"
description: "Installs and builds"
runs:
  using: "composite"
  steps:
    - uses: pnpm/action-setup@v4
    - run: pnpm install --frozen-lockfile
      shell: bash
    - run: pnpm build
      shell: bash
